<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.superfit">
    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:allowBackup="true"
        android:icon="@drawable/appicon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/appicon"
        android:supportsRtl="true"
        android:theme="@style/Theme.SuperFit">
        <activity android:name=".registration_activity">

            <intent-filter>

                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".one_autorization_avtivity"/>
        <activity android:name=".main_activity"/>
        <activity android:name=".splash_activity"/>
        <activity android:name=".autorization_activity"/>
        <activity android:name=".ResipesList"/>
        <activity android:name=".RecipesList_Item"/>
        <activity android:name=".RecipeScreenActivity"></activity>


    </application>

</manifest>
